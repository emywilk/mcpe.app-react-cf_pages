{
  "name": "mcpe-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev-react": "react-scripts start",
    "build": "react-scripts build && cp -r functions build/",
    "dev": "npm run build && wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy ./build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-helmet": "^6.1.0",
    "react-router-dom": "^6.28.0",
    "react-scripts": "5.0.1",
    "swiper": "^11.1.15"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.15"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
